import "./App.css";

function App() {
  return (
    <div className="App">
      <div className='instruction'>
        Create a folder with your Registration Number inside <em>src -{">"}{" "}
        components</em>, and inside that create your components the way you want
        to(either a single jsx file for each component, or a folder having js/jsx &
        css/sass files). Import and add your components below {"<h1>"}.
      </div>
    </div>
  );
}

export default App;
